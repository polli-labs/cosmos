# POL-2094 authority probe

Temporary, non-merging candidate used to prove that the public Cosmos `main`
ruleset blocks while `build-test (3.12)` is pending and permits the same exact
head once that GitHub Actions check is green.
